{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "main": "./lib/index.js",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mysql": "^2.18.1",
    "serverless-http": "^2.6.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.7",
    "@types/express": "^4.17.8",
    "@types/mysql": "^2.15.15",
    "serverless": "^2.3.0",
    "serverless-ignore": "^0.1.5",
    "typescript": "^4.0.3"
  },
  "scripts": {
    "deploy": "yarn tsc && npx serverless deploy --aws-profile edu # Use AWS Educate profile",
    "lint": "cd .. && yarn lint && cd server",
    "lint:fix": "cd .. && yarn lint:fix && cd server",
    "tsc": "npx tsc -p .",
    "start": "./start --dev # Make sure to chmod +x this or it will fail"
  }
}
